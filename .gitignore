node_modules/
dist/
*.log
.env

# personal config (may contain wallet addresses) — keep the example tracked
vexis.config.json

# personal deploy script (contains VPS address) — keep server info out of the repo
scripts/deploy.sh

# telegram alert state (per-machine runtime state)
.vexis-alerts.json

# watchlist state (per-machine runtime state)
.vexis-watchlist.json

# stop-loss / take-profit rules (per-machine runtime state)
.vexis-tpsl.json

# agent runtime state (autonomous trading loop)
.vexis-agent.json
.vexis-agent-journal.jsonl
.vexis-agent-signals.json
.vexis-portfolio-history.json
.vexis-agent-narrative.json
